apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mysql-db-users
  name: mysql-db-users
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
